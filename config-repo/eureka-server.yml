info:
  app: Eureka Server
  description: Servidor de configuración centralizada
  version: 1.0.0

management:
  endpoints:
    web:
      exposure:
        include: health, info

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

server:
  port: 8761
